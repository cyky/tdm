service:
  name: aws-nodejs-typescript

plugins:
  - serverless-webpack
  - serverless-offline

provider:
  name: aws
  runtime: nodejs8.10
  region: eu-west-1

functions:
  singleScraper:
    handler: singleScraper.scrape
    events:
      - http:
          method: get
          path: hello
